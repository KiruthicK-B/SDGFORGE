import 'package:flutter/material.dart';

class SellProduce extends StatelessWidget {
  const SellProduce({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Buy Products")),
      body: Center(child: Text("Buy Products Page")),
    );
  }
}
    